@model MvcApplication2.Models.HataliOperasyon

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>HataliOperasyon</legend>

        @Html.HiddenFor(model => model.HataliOperasyonId)

        <div class="editor-label">
            @Html.LabelFor(model => model.OperasyonAdi)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.OperasyonAdi)
            @Html.ValidationMessageFor(model => model.OperasyonAdi)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
