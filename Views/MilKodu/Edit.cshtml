@model MvcApplication2.Models.MilTipi

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>MilTipi</legend>

        @Html.HiddenFor(model => model.MilTipiId)

        <div class="editor-label">
            @Html.LabelFor(model => model.MilTipiIsmi)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.MilTipiIsmi)
            @Html.ValidationMessageFor(model => model.MilTipiIsmi)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
