@model IEnumerable<MvcApplication2.Models.EstasKoduTipi>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EstasKoduTipiStr)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.EstasKoduTipiStr)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EstasKoduTipiId }) |
            @Html.ActionLink("Details", "Details", new { id=item.EstasKoduTipiId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EstasKoduTipiId })
        </td>
    </tr>
}

</table>
