@model MvcApplication2.Models.EstasKoduTipi

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>EstasKoduTipi</legend>

        @Html.HiddenFor(model => model.EstasKoduTipiId)

        <div class="editor-label">
            @Html.LabelFor(model => model.EstasKoduTipiStr)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.EstasKoduTipiStr)
            @Html.ValidationMessageFor(model => model.EstasKoduTipiStr)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
