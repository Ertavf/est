@model MvcApplication2.Models.Firma

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Firma</legend>

        @Html.HiddenFor(model => model.FirmaId)

        <div class="editor-label">
            @Html.LabelFor(model => model.FirmaIsmi)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.FirmaIsmi)
            @Html.ValidationMessageFor(model => model.FirmaIsmi)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.FirmaNumarasi)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.FirmaNumarasi)
            @Html.ValidationMessageFor(model => model.FirmaNumarasi)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
