@model IEnumerable<MvcApplication2.Models.Sanayi>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SanayiTipi)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SanayiTipi)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SanayiId }) |
            @Html.ActionLink("Details", "Details", new { id=item.SanayiId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SanayiId })
        </td>
    </tr>
}

</table>
