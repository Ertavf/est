@model MvcApplication2.Models.HataSebebi

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>HataSebebi</legend>

        @Html.HiddenFor(model => model.HataSebebiId)

        <div class="editor-label">
            @Html.LabelFor(model => model.Sebep)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Sebep)
            @Html.ValidationMessageFor(model => model.Sebep)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
